<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.gatewayservice.dao.UserMapper">

    <select id="getUserInfo" resultType="com.example.gatewayservice.dto.UserDTO">
        select * from users where user_rep_id = #{user_rep_id}
    </select>

</mapper>